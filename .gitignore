# Local packages and production build output
node_modules/
.next/
out/
tsconfig.tsbuildinfo

# Private local settings
.claude/settings.local.json
.env
.env.local
.env.*.local

# System files
.DS_Store

# The visual suite is local scaffolding for the CSS migration, not repo cargo.
# It needs a live Supabase session and machine-specific font rendering, so its
# baselines could never match between two machines or in CI. It stays on disk;
# MIGRATION.md says how to run it and what to reinstall first.
tests/visual/
playwright.config.ts
test-results/
playwright-report/

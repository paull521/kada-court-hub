NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Fallback origin for auth email links (sign-up confirmation). The request
# Origin header is used when present, so this only matters where that is absent.
NEXT_PUBLIC_SITE_URL=
